1. 安装 GSAP 动画库到 wxmlyq-space 应用中
2. 修改 `loveletter.tsx` 文件：

   * 移除现有的 `useState` 和 `useEffect` 定时器逻辑

   * 导入 GSAP 和 ScrollTrigger

   * 使用 GSAP 的 ScrollTrigger 实现滚动触发的句子显示动画

   * 为每个句子添加淡入效果，随着滚动逐渐显示
3. 确保动画效果流畅，符合原设计的渐进式显示风格

